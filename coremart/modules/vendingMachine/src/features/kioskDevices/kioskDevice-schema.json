{
	"name": "kioskDevice",
	"fields": {
		"id": {
			"type": "string",
			"label": "{ \"$ref\": 'coremart.vendingMachine.device.fields.id' }",
			"hidden": true
		},
		"code": {
			"type": "string",
			"label": "{ \"$ref\": 'coremart.vendingMachine.device.fields.code' }"
		},
		"name": {
			"type": "string",
			"label": "{ \"$ref\": 'coremart.vendingMachine.device.fields.name' }"
		},
		"status": {
			"type": "enum",
			"label": "{ \"$ref\": 'coremart.vendingMachine.device.fields.status' }",
			"enum": [
				{
					"value": "active",
					"label": "{ \"$ref\": 'nikki.general.status.active' }"
				},
				{
					"value": "inactive",
					"label": "{ \"$ref\": 'nikki.general.status.inactive' }"
				}
			]
		},
		"deviceType": {
			"type": "enum",
			"label": "{ \"$ref\": 'coremart.vendingMachine.device.fields.deviceType' }",
			"enum": [
				{
					"value": "motor",
					"label": "{ \"$ref\": 'coremart.vendingMachine.device.deviceType.motor' }"
				},
				{
					"value": "pos",
					"label": "{ \"$ref\": 'coremart.vendingMachine.device.deviceType.pos' }"
				},
				{
					"value": "screen",
					"label": "{ \"$ref\": 'coremart.vendingMachine.device.deviceType.screen' }"
				},
				{
					"value": "cpu",
					"label": "{ \"$ref\": 'coremart.vendingMachine.device.deviceType.cpu' }"
				},
				{
					"value": "router",
					"label": "{ \"$ref\": 'coremart.vendingMachine.device.deviceType.router' }"
				}
			]
		},
		"description": {
			"type": "string",
			"label": "{ \"$ref\": 'coremart.vendingMachine.device.fields.description' }"
		},
		"specifications": {
			"type": "array",
			"label": "{ \"$ref\": 'coremart.vendingMachine.device.fields.specifications' }"
		},
		"createdAt": {
			"type": "date",
			"label": "{ \"$ref\": 'coremart.vendingMachine.device.fields.createdAt' }",
			"hidden": true
		}
	}
}
